#include <stdio.h>

/**
* main - Print the numbers of base 16 in lowercase
* Return: Always 0 (Success)
*/
int main(void)
{
		char h;

		for (h = '0'; h <= '9'; h++)
		putchar(digit);

		for (h = 'a'; h <= 'f'; h++)
		putchar(h);

		putchar('\n');

	return (0);
}
